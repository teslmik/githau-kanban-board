type ItemType = {
  id: number;
  user: string;
  comments: number;
  number: number;
  title: string;
  state: string;
  assignee: string;
  createdAt: string;
};

export type { ItemType };
